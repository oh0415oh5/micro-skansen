version: '3.9'

services:
  mcp-server:
    build: .
    container_name: micro-skansen-mcp
    env_file:
      - .env
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    restart: unless-stopped
